# 영상 디자이너 포트폴리오 사이트

영상 디자이너를 위한 현대적이고 아름다운 포트폴리오 웹사이트입니다.

## 기능

- ✨ 현대적이고 반응형 디자인
- 🎬 영상 디자이너에 최적화된 섹션
- 🎨 부드러운 애니메이션 (Framer Motion)
- 📱 모바일 친화적
- 🚀 빠른 로딩 속도 (Vite)

## 시작하기

### 필요한 것

- Node.js (v16 이상)
- npm 또는 yarn

### 설치

```bash
cd F:\video-designer-portfolio
npm install
```

### 개발 서버 실행

```bash
npm run dev
```

브라우저에서 http://localhost:5173 을 열어 확인하세요.

### 빌드

프로덕션 빌드를 만들려면:

```bash
npm run build
```

빌드된 파일은 `dist` 폴더에 생성됩니다.

### 미리보기

빌드된 사이트를 미리보려면:

```bash
npm run preview
```

## 프로젝트 구조

```
video-designer-portfolio/
├── src/
│   ├── components/      # React 컴포넌트
│   │   ├── Header.jsx
│   │   ├── Hero.jsx
│   │   ├── About.jsx
│   │   ├── Skills.jsx
│   │   ├── Portfolio.jsx
│   │   ├── Contact.jsx
│   │   └── Footer.jsx
│   ├── App.jsx          # 메인 App 컴포넌트
│   ├── App.css          # 스타일
│   ├── index.css        # 전역 스타일
│   └── main.jsx         # 진입점
├── index.html
├── package.json
├── vite.config.js
└── README.md
```

## 커스터마이징

### 색상 변경

`src/index.css` 파일의 CSS 변수를 수정하여 색상을 변경할 수 있습니다:

```css
:root {
  --primary-color: #6366f1;
  --secondary-color: #8b5cf6;
  --accent-color: #ec4899;
}
```

### 내용 수정

각 컴포넌트 파일에서 텍스트, 이미지, 기술 스택 등을 수정할 수 있습니다.

### 포트폴리오 프로젝트 추가

`src/components/Portfolio.jsx` 파일의 `projects` 배열에 새 프로젝트를 추가하세요.

## 기술 스택

- React 18
- Vite
- Framer Motion
- CSS3

## 라이선스

MIT License

